# The template for .env.local. Copy this file to .env.local and fill it in.
#
# SECRETS ONLY. Everything in here is a credential, which is the rule that decides
# where a variable goes: non-secret settings shared by every deployment live in .env,
# which is committed, and this file is the half that must never be. ENV and
# SPOTIFY_PLAYLIST_ID are in .env for that reason — neither is a secret.
#
# Nothing here has a default worth shipping, so every value below is blank rather than
# an example. A blank one degrades a feature; see the note above each.

# Spotify credentials for the now_playing panel.
# See docs/spotify-setup.md.
# Without these the panel renders its "offline" state; nothing else breaks.

SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_REFRESH_TOKEN=

# The write credential, for /lab/suggest only. TWO TOKENS ON PURPOSE.
#
# Spotify's scopes are verbs rather than resources, so no token can be limited to one
# playlist. The intent is that only the route which adds a track holds this one.
#
# BUT THE READ TOKEN IS NOT ACTUALLY READ-ONLY, which is worth knowing before relying
# on it. Spotify grants scopes per application rather than per token, so approving
# playlist-modify-public widened the token above too. "pnpm token:check" reports it.
# Real separation needs a second Spotify app; see docs/spotify-setup.md.
#
# playlist-modify-PRIVATE is never requested by either, so neither can reach a private
# playlist.
#
# Mint it with: node scripts/spotify-token.mjs <id> <secret> write
# Without it the suggestion page works and adding refuses. Nothing else notices.

SPOTIFY_WRITE_REFRESH_TOKEN=

# Resend API key for the contact form — see docs/contact-setup.md.
# The sender and recipient addresses are not secret and do not vary by deployment,
# so they live in src/constants/contact.ts, not here.
# Without this /api/contact answers 503 and the form tells the visitor to use the
# e-mail link instead.

RESEND_API_KEY=

# ENV and SPOTIFY_PLAYLIST_ID used to be here. They are not secrets, so they moved to
# .env, which is committed. Set them on the host to override, not in .env.local.

# Neon Postgres, shared by the lab apps — see docs/neon-setup.md.
# The POOLED connection string, the one with -pooler in the host.
# Without it the suggestion list still renders, just without names, and adds refuse.

DATABASE_URL=

# last.fm, for /lab/deepcuts only — see docs/lab.md.
#
# THE ONLY UPSTREAM ON THIS SITE THAT IS NOT SPOTIFY, and it exists because of a hard
# limit rather than a preference: the Spotify Web API has never exposed a play count,
# and `popularity` — the one field that came close — is now deprecated. Every card on
# /lab/deepcuts is scored on how FEW plays a track has, so that number has to come from
# somewhere else.
#
# Free, instant, and not an OAuth flow: create an API account at
# https://www.last.fm/api/account/create and copy the "API key" (not the shared secret,
# which nothing here uses).
#
# Without it the packs still open and still list their songs; every track reads
# "unmatched" and nothing is scored. The panel says so rather than pretending.

LASTFM_API_KEY=
